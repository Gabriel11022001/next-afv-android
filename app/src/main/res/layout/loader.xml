<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/white"
    android:paddingStart="10dp"
    android:paddingEnd="10dp"
    android:paddingTop="15dp"
    android:paddingBottom="10dp" >

    <ProgressBar
        android:id="@+id/loader_progress"
        android:layout_width="20dp"
        android:layout_height="20dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:indeterminate="true" />

    <TextView
        android:id="@+id/txt_mensagem_loader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Carregando, aguarde..."
        app:layout_constraintStart_toEndOf="@id/loader_progress"
        app:layout_constraintTop_toTopOf="@id/loader_progress"
        app:layout_constraintBottom_toBottomOf="@id/loader_progress"
        android:layout_marginStart="10dp"
        android:textColor="@color/principal"
        android:textStyle="bold"
        android:textSize="14sp" />

</androidx.constraintlayout.widget.ConstraintLayout>