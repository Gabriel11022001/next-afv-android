<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- redirecionar usuário para tela de "sobre" -->
    <item
        android:id="@+id/menu_item_sobre"
        android:title="@string/txt_menu_sobre"
        app:showAsAction="never" />

    <!-- opção faz logout do usuário logado -->
    <item
        android:id="@+id/menu_item_logout"
        android:title="@string/txt_menu_logout"
        app:showAsAction="never" />

    <!-- opção redirecionar tela notificações -->
    <item
        android:id="@+id/menu_item_notificacoes"
        android:title="Notificações"
        app:showAsAction="never" />

</menu>